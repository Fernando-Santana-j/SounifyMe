<!DOCTYPE html>
<html >
<head>
    
    <title>Google Login</title>
    <%- include('./reusable/head.ejs') %>
    <link rel="stylesheet" href="../../public/css/reusable/styleRoot.css">
</head>
<body>
    <style>
        :root{
            --popup-background-color: rgba(255, 255, 255,0.9);
            
        }
        [data-theme="light"]{
            --popup-background-color: rgba(40, 40, 40,0.9);
        }
        body,html{
            width: 100%;
            height: 100%;
        }
        #popup-containner{
            width: 100%;
            height: 100%;
            display: flex;
            justify-content: center;
            align-items: center;
        }
        #popup-content{
            width: 30vw;
            height: 40vh;
            border-radius: 20px;
            background-color: var( --popup-background-color);
            display: flex;
            align-items: center; 
            padding: 1em;   
            flex-direction: column;
            justify-content: space-between;
        }
        #title{
            color: var( --primary-color);
            text-align: center;
            
            font: 500 1.5em 'inter';
            line-height: 1.2em;
            margin-top: 2em;
        }
        #buttons{
            margin-bottom: 2em;

        }
        #buttons button{
            padding: 15px;
            border-radius: 10px;
            border: none;
            background-color: var(--color-text-primary);
            color: var(--primary-color);
            font: 400 1em 'inter';
            margin-right: 1em;
            transition: 0.6s;
        }
        #buttons button:hover{
            opacity: 0.9;
            transition: 0.6s;
            cursor: pointer;
        }
    </style>
    <div id="popup-containner">
        <div id="popup-content">
            <h1 id="title">Detectamos que o e-mail que você esta acessando está cadastrado no Google, devido a isso por questões de segurança pedimos para se conectar novamente com sua conta do Google!</h1>
            <div id="buttons">
                <button id="cancelar">Voltar para a pagina de login</button>
                <button id="aceitar">Logar-se com o Google</button>
            </div>
        </div>
    </div>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.11.0/jquery.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/crypto-js/4.0.0/crypto-js.min.js"></script>
    <script src="https://accounts.google.com/gsi/client" ></script>
    <script type="module" src="../../public/js/google-login.js"></script>
</body>
</html>